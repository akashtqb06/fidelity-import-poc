{
  "name": "poc-import",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone POC for Component Import Fidelity — LMNAs Studio",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "demo": "npx serve demo -p 4321 --no-clipboard"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
